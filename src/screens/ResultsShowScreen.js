import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ResultsShowScreen = ({prop}) =>{
    return <View>
<Text>Results Show {prop}</Text>
    </View>
};

const styles = StyleSheet.create({})

export default ResultsShowScreen;